imports:
    - { resource: ../common/config.yml }

#framework:
#    validation:
#        cache: apc

#doctrine:
#    orm:
#        metadata_cache_driver: apc
#        result_cache_driver: apc
#        query_cache_driver: apc

monolog:
    handlers:
        main:
            type:         fingers_crossed
            action_level: error
            handler:      nested
        nested:
            type:  stream
            path:  "php://stderr"
            level: debug
        console:
            type:  console

# Assetic Configuration
assetic:
    filters:
        less:
            node: /app/.heroku/node/bin/node
            node_paths: [/app/node_modules]
        compass:
            sass: /app/vendor/bundle/bin/compass