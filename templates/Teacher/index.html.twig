{% extends "layout.html.twig" %}

{% block headline %}
    {{ 'title.teachers'|trans() }}
{% endblock %}

{% block content_content %}
<div class="table-responsive">
    <table class="table table-striped table-hover table-sm">
        <thead>
            <tr>
                <th scope="col" data-field="id" data-align="right" data-sortable="true">ID</th>
                <th scope="col" data-field="name" data-align="right" data-sortable="true">{{ 'form.label.name'|trans() }}</th>
                <th scope="col" data-field="lastname" data-align="right" data-sortable="true">{{ 'form.label.lastname'|trans() }}</th>
                <th scope="col" data-field="email" data-align="right" data-sortable="true">{{ 'form.label.email'|trans() }}</th>
                <th scope="col" data-field="wage" data-align="center" data-sortable="true">{{ 'form.label.wage'|trans() }}</th>
                <th scope="col" data-field="comment" data-sortable="true">{{ 'form.label.comment'|trans() }}</th>
                <th scope="col" data-field="show" data-sortable="false"></th>
            </tr>
        </thead>
        <tbody>
        {% for teacher in teachers %}
            <tr class="tr-class-1">
                <th scope="row" class="info">{{ teacher.id }}</th>
                <td>{{ teacher.User.name }}</td>
                <td>{{ teacher.User.surname }}</td>
                <td>{{ teacher.User.email }}</td>
                <td>{{ teacher.wage }}</td>
                <td>{{ teacher.comment }}</td>
                <td class="info"><a href="{{ path('danceschool_teacher_show', {'id': teacher.id }) }}" class="btn btn-primary btn-sm" role="button">{{ 'button.view'|trans() }}</a></td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
</div>
{% endblock %}
