{% extends "layout.html.twig" %}

{% block headline %}
    {{ 'title.list_students'|trans() }}
{% endblock %}

{% block content_content %}
<div>
    <a href="{{ path('danceschool_student_add') }}" class="btn btn-primary" role="button">{{ 'button.add'|trans({}) }}</a>
</div>
<div class="table-responsive">
    <table class="table table-striped table-hover table-sm">
        <thead>
            <tr>
                <th scope="col" data-field="id" data-align="right" data-sortable="true">ID</th>
                <th scope="col" data-field="name" data-align="right" data-sortable="true">{{ 'form.label.name'|trans() }}</th>
                <th scope="col" data-field="lastname" data-align="right" data-sortable="true">{{ 'form.label.lastname'|trans() }}</th>
                <th scope="col" data-field="email" data-align="right" data-sortable="true">{{ 'form.label.email'|trans() }}</th>
                <th scope="col" data-field="comment" data-sortable="true">{{ 'form.label.comment'|trans() }}</th>
                <th scope="col" data-field="show" data-sortable="false"></th>
                <th scope="col" data-field="edit" data-sortable="false"></th>
                <th scope="col" data-field="edit" data-sortable="false"></th>
                <th scope="col" data-field="delete" data-sortable="false"></th>
            </tr>
        </thead>
        <tbody>
        {% for student in students %}
            <tr class="tr-class-1">
                <th scope="row" class="info">{{ student.id }}</td>
                <td>{{ student.user.name }}</td>
                <td>{{ student.user.lastname }}</td>
                <td>{{ student.user.email }}</td>
                <td>{{ student.comment }}</td>
                <td class="info"><a href="{{ path('danceschool_student_show', {'id': student.id }) }}" class="btn btn-primary btn-sm" role="button">{{ 'button.view'|trans({}) }}</a></td>
                <td class="info"><a href="{{ path('danceschool_student_edit', {'id': student.id }) }}" class="btn btn-primary btn-sm" role="button">{{ 'button.edit'|trans() }}</a></td>
                <td class="danger"><a href="{{ path('danceschool_student_remove', {'id': student.id }) }}" class="btn btn-danger btn-sm" role="button">{{ 'button.delete'|trans() }}</a></td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
</div>
{% endblock %}
