extensions:
    - PhpSpec\Extension\CodeCoverageExtension

code_coverage:
    output: build/coverage_spec.cov
    format: php
    whitelist: [src]
    blacklist:
        - tests

formatter.name: pretty
