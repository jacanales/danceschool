{% extends "::layout.html.twig" %}

{% block headline %}
    {{ 'title.students'|trans({}, 'AppBundle') }}
{% endblock %}

{% block content_content %}
    {{ form_start(form, {'action': path('danceschool_student_annotation_add', {'studentId': studentId })}) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
{% endblock %}
