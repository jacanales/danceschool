{% extends "AppBundle::layout.html.twig" %}

{% block headline %}
    {{ 'title.groups'|trans({}, 'AppBundle') }}
{% endblock %}

{% block content_content %}
    {{ form_start(form, {'action': path('danceschool_group_add')}) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
{% endblock %}
