{% extends "AppBundle::layout.html.twig" %}

{% block headline %}
    {{ 'title.courses'|trans({}, 'AppBundle') }}
{% endblock %}

{% block content_content %}
    {{ form_start(form, {'action': path('danceschool_course_edit', {'id': course.getId() })}) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
{% endblock %}
